apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: banka-platform
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
