global:
  resolve_timeout: 5m

route:
  receiver: discord
  group_by: ["alertname", "service", "job"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: discord
    webhook_configs:
      - url: http://discord_notifier:8080/alerts
        send_resolved: true
